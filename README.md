# Setup

Install same dependencies as HW2 plus the following

+ pybids
+ nilearn

There is a notebook in the HW2 folder called `BIDS.ipynb` that outlines how to process the BIDS dataset and retrieve the time-aligned stimuli. There is also a `utils.py` file containing useful functions.